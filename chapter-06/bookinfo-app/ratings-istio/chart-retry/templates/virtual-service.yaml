{{- $vFaulty50percent503 := 0 }}
{{- $vInternalServerError500 := 0 }}
{{- $v2 := 0 }}

{{- if eq .Values.retry.by503StatusCode.enabled true }}
{{- $vFaulty50percent503 = 100 }}
{{- else if eq .Values.retry.by500StatusCode.enabled true }}
{{- $vInternalServerError500 = 100 }}
{{- else }}
{{- $v2 = 100 }}
{{- end }}

apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: {{ .Values.service.name }}
spec:
  exportTo:
    - "."
  hosts:
    - {{ .Values.service.host }}
  http:
    - match:
        - port: {{ .Values.service.port }}
      route:
        - destination:
            host: {{ .Values.service.host }}
            port:
              number: {{ .Values.service.port }}
            subset: v1
          weight: 0
        - destination:
            host: {{ .Values.service.host }}
            port:
              number: {{ .Values.service.port }}
            subset: v2
          weight: {{ $v2 }}
        - destination:
            host: {{ .Values.service.host }}
            port:
              number: {{ .Values.service.port }}
            subset: v-delayed-50percent-delayof7s
          weight: 0
        - destination:
            host: {{ .Values.service.host }}
            port:
              number: {{ .Values.service.port }}
            subset: v-faulty-50percent-503
          weight: {{ $vFaulty50percent503 }}
        - destination:
            host: {{ .Values.service.host }}
            port:
              number: {{ .Values.service.port }}
            subset: v-internal-server-error-500
          weight: {{ $vInternalServerError500 }}
        - destination:
            host: {{ .Values.service.host }}
            port:
              number: {{ .Values.service.port }}
            subset: v-unavailable-503-in60s
          weight: 0
      {{- if or (eq .Values.retry.by503StatusCode.enabled true) (eq .Values.retry.by500StatusCode.enabled true) }}
      retries:
        attempts: 3
        perTryTimeout: 10s
        retryOn: connect-failure,reset-before-request,5xx
      {{- else }}
      retries:
        attempts: 3
        perTryTimeout: 10s
        retryOn: connect-failure,reset-before-request
      {{- end }}
