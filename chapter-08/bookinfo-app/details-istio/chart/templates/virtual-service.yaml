apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: {{ .Values.service.name }}
spec:
  exportTo:
    - "."
  hosts:
    - {{ .Values.service.host }}
  http:
    - match:
        - port: {{ .Values.service.port }}
      route:
        - destination:
            host: {{ .Values.service.host }}
            port:
              number: {{ .Values.service.port }}
            subset: v1
          weight: 0
        - destination:
            host: {{ .Values.service.host }}
            port:
              number: {{ .Values.service.port }}
            subset: v2
          weight: 100
      timeout: 10s
      retries:
        attempts: 3
        perTryTimeout: 10s
        retryOn: 5xx
---
apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: {{ .Values.service.name }}-egress
spec:
  exportTo:
    - "*"
  hosts:
    - {{ .Values.googleapis.host }}
  gateways:
    - mesh
    - {{ .Values.googleapis.egressGatewayName }}
  tcp:
    - match:
        - gateways:
            - mesh
          port: {{ .Values.googleapis.port }}
      route:
        - destination:
            host: istio-egressgateway.istio-egress.svc.cluster.local
            port:
              number: {{ .Values.googleapis.port }}
    - match:
        - gateways:
            - {{ .Values.googleapis.egressGatewayName }}
          port: {{ .Values.googleapis.port }}
      route:
        - destination:
            host: {{ .Values.googleapis.host }}
            port:
              number: {{ .Values.googleapis.port }}
