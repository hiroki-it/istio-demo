apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: {{ .Values.service.name }}-http
spec:
  exportTo:
    - "*"
  hosts:
    - {{ .Values.googleapis.host }}
  location: MESH_EXTERNAL
  ports:
    - number: {{ .Values.googleapis.port }}
      name: https-{{ .Values.service.name }}
      protocol: HTTPS
  resolution: DNS
