apiVersion: telemetry.istio.io/v1
kind: Telemetry
metadata:
  name: {{ .Release.Namespace }}
spec:
  accessLogging:
    - disabled: true
  metrics:
    - overrides:
        - disabled: true
  tracing:
    - disableSpanReporting: true
