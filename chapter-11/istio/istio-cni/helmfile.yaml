repositories:
  - name: istio
    url: https://istio-release.storage.googleapis.com/charts

releases:
  - name: istio-cni
    chart: istio/cni
    version: 1.25.3
    namespace: kube-system
    atomic: true
    values:
      - values.yaml
