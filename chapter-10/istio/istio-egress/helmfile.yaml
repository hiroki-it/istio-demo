repositories:
  - name: istio
    url: https://istio-release.storage.googleapis.com/charts

releases:
  - name: istio-egressgateway
    chart: istio/gateway
    version: 1.24.3
    namespace: istio-egress
    atomic: true
    values:
      - values.yaml
