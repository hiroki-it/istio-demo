repositories:
  - name: istio
    url: https://istio-release.storage.googleapis.com/charts

releases:
  - name: istio-base
    chart: istio/base
    version: 1.25.0
    namespace: istio-system
    atomic: true
    values:
      - values.yaml
