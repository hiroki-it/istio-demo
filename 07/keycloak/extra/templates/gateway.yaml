apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: {{ .Values.serviceName }}-mysql-egress
spec:
  selector:
    istio: egressgateway
  servers:
    - port:
        name: tcp-{{ .Values.serviceName }}
        protocol: TCP
        number: 3306
      hosts:
        - "*"

