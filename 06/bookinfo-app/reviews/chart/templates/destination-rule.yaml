apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: {{ .Values.serviceName }}
spec:
  exportTo:
    - "*"
  host: {{ .Values.serviceName }}
  subsets:
    - name: v3
      labels:
        version: v3
