repositories:
  - name: minio
    url: https://charts.min.io/

releases:
  - name: minio
    chart: minio/minio
    version: 5.4.0
    namespace: istio-system
    atomic: true
    values:
      - values.yaml
