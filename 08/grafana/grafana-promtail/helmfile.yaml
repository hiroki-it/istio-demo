repositories:
  - name: grafana
    url: https://grafana.github.io/helm-charts

releases:
  - name: grafana-promtail
    chart: grafana/promtail
    version: 3.2.0
    namespace: default
    values:
      - values.yaml
