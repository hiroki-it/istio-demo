repositories:
  - name: grafana
    url: https://grafana.github.io/helm-charts

releases:
  - name: grafana-tempo
    chart: grafana/tempo
    version: 1.16.0
    namespace: istio-system
    values:
      - values.yaml
