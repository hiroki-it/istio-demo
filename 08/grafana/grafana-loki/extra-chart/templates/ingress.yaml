apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.grafanaLoki.name }}
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.grafanaLoki.name }}
                port:
                  number: {{ .Values.grafanaLoki.port }}
