apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.kiali.name }}
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.kiali.name }}
                port:
                  number: {{ .Values.kiali.port }}
