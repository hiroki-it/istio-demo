repositories:
  - name: grafana
    url: https://grafana.github.io/helm-charts

releases:
  - name: grafana
    chart: grafana/grafana
    version: 8.13.2
    namespace: grafana
    atomic: true
    values:
      - values.yaml

