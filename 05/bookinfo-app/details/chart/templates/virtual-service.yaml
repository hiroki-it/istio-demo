apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: {{ .Values.serviceName }}
spec:
  exportTo:
    - "*"
  hosts:
    - {{ .Values.serviceName }}
  http:
    - match:
        - port: 9080
      route:
        - destination:
            host: {{ .Values.serviceName }}
            port:
              number: 9080
            subset: v1
