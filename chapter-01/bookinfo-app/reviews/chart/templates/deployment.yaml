apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.service.name }}-v1
  labels:
    app: {{ .Values.service.name }}
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.service.name }}
      version: v1
  template:
    metadata:
      labels:
        app: {{ .Values.service.name }}
        version: v1
    spec:
      serviceAccountName: bookinfo-{{ .Values.service.name }}
      containers:
        - name: {{ .Values.service.name }}
          image: docker.io/istio/examples-bookinfo-{{ .Values.service.name }}-v1:1.20.2
          imagePullPolicy: IfNotPresent
          env:
            - name: LOG_DIR
              value: "/tmp/logs"
          ports:
            - containerPort: {{ .Values.service.port }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: wlp-output
              mountPath: /opt/ibm/wlp/output
      volumes:
        - name: wlp-output
          emptyDir: {}
        - name: tmp
          emptyDir: {}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.service.name }}-v2
  labels:
    app: {{ .Values.service.name }}
    version: v2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.service.name }}
      version: v2
  template:
    metadata:
      labels:
        app: {{ .Values.service.name }}
        version: v2
    spec:
      serviceAccountName: bookinfo-{{ .Values.service.name }}
      containers:
        - name: {{ .Values.service.name }}
          image: docker.io/istio/examples-bookinfo-{{ .Values.service.name }}-v2:1.20.2
          imagePullPolicy: IfNotPresent
          env:
            - name: LOG_DIR
              value: "/tmp/logs"
          ports:
            - containerPort: {{ .Values.service.port }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: wlp-output
              mountPath: /opt/ibm/wlp/output
      volumes:
        - name: wlp-output
          emptyDir: {}
        - name: tmp
          emptyDir: {}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.service.name }}-v3
  labels:
    app: {{ .Values.service.name }}
    version: v3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.service.name }}
      version: v3
  template:
    metadata:
      labels:
        app: {{ .Values.service.name }}
        version: v3
    spec:
      serviceAccountName: bookinfo-{{ .Values.service.name }}
      containers:
        - name: {{ .Values.service.name }}
          image: docker.io/istio/examples-bookinfo-{{ .Values.service.name }}-v3:1.20.2
          imagePullPolicy: IfNotPresent
          env:
            - name: LOG_DIR
              value: "/tmp/logs"
          ports:
            - containerPort: {{ .Values.service.port }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: wlp-output
              mountPath: /opt/ibm/wlp/output
      volumes:
        - name: wlp-output
          emptyDir: {}
        - name: tmp
          emptyDir: {}
